---
import Layout from '../layouts/Layout.astro';
import bgImage from '../assets/images/forest-bg.jpg';
---

<Layout title="404: Territorio No Explorado | Paulina Lopez" description="Parece que te has perdido en el bosque. Esta página no existe.">
  
  <section class="relative min-h-[80vh] flex items-center justify-center overflow-hidden">
    
    {/* FONDO AMBIENTADO */}
    <div class="absolute inset-0 bg-midnight z-0"></div>
    <div 
        class="absolute inset-0 opacity-10 mix-blend-overlay bg-cover bg-center z-0"
        style={{ backgroundImage: `url(${bgImage.src})` }}
    ></div>
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-transparent via-midnight to-midnight z-0"></div>

    {/* Agregué 'relative' para asegurar el contexto, pero el z-index es clave */}
    <div class="container mx-auto px-6 relative z-20 text-center">
      
      {/* NÚMERO DE ERROR ESTILO GLITCH */}
      <h1 class="font-gothic text-9xl md:text-[12rem] text-transparent bg-clip-text bg-gradient-to-b from-blood to-red-900 animate-pulse mb-4 select-none">
        404
      </h1>

      {/* MENSAJE TEMÁTICO */}
      <h2 class="font-gothic text-3xl md:text-4xl text-gold mb-6 tracking-widest uppercase">
        Territorio No Explorado
      </h2>

      <p class="text-gray-400 text-lg max-w-2xl mx-auto mb-10 leading-relaxed">
        Parece que te has desviado del sendero seguro. Esta ubicación no aparece en los mapas del 
        <span class="text-white font-bold"> Castillo Hayashi</span> ni en los registros oficiales.
        <br/><br/>
        No te quedes sola en el bosque... regresa antes de que anochezca.
      </p>

      {/* BOTONES DE ACCIÓN */}
      <div class="flex flex-col md:flex-row gap-6 justify-center items-center">
        
        {/* CORRECCIÓN 1: inline-flex para estructura sólida */}
        <a href="/" class="inline-flex items-center justify-center px-8 py-3 bg-white/5 border border-white/10 text-white rounded hover:bg-white/10 hover:border-gold/50 transition duration-300 backdrop-blur-sm group">
          <span class="mr-2">←</span> 
          <span class="group-hover:text-gold transition">Volver al Inicio</span>
        </a>

        <a href="/#libros" class="inline-flex items-center justify-center px-8 py-3 bg-blood text-white font-bold rounded hover:bg-red-700 transition duration-300 shadow-[0_0_20px_rgba(168,0,0,0.4)]">
          VER LIBROS DISPONIBLES
        </a>

      </div>

    </div>

    {/* ELEMENTO DECORATIVO FLOTANTE */}
    {/* CORRECCIÓN 2: pointer-events-none */}
    <div class="absolute bottom-0 w-full h-32 bg-gradient-to-t from-black to-transparent z-10 pointer-events-none"></div>
  </section>

</Layout>